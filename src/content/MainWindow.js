import React from "react";
import { Box, AppBar, Toolbar, Typography } from "@mui/material";

export const MainWindow = () => {
    return (
        <Box>
            <AppBar className={"App-Header"}>SHOPPING LIST</AppBar>
        </Box>
    );
}